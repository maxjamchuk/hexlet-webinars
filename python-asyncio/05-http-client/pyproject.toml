[project]
name = "service-status-http-client"
version = "0.1.0"
description = "Конкурентные HTTP-проверки локальных сервисов"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27,<1",
]

[project]
name = "service-status-limited-concurrency"
version = "0.1.0"
description = "Ограничение конкурентных HTTP-проверок"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27,<1",
]

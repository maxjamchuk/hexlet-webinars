# 09 — TDD: Green

## Цель

Минимальным изменением сделать новый и старые тесты зелёными.

## Что изменилось

В `inspect_cargo()` добавлен отдельный `if` для `alien_sample`; намеренное повторение пока сохранено.

## Файлы для показа

- `customs.py` — два последовательных карантинных условия;
- `tests/test_inspect_cargo.py` — требование из шага Red.

## Подготовка

Все команды выполняются из каталога `09-tdd-green`.

```bash
uv sync --locked
```

## Запуск

```bash
uv run pytest -v
```

## Ожидаемый результат

Новый и все прежние тесты проходят: `13 passed`. Код процесса `0`.

## Что важно объяснить

На шаге Green внесено минимальное изменение. Повторение допустимо временно: сначала подтверждается поведение, а улучшение структуры выполняется отдельным шагом.

## Ограничение текущего решения

Два условия возвращают один статус и дублируют идею; следующая стадия выполняет рефакторинг под защитой тестов.


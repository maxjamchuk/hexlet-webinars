[project]
name = "space-customs-01-first-test"
version = "0.1.0"
description = "First pytest example for the Space Customs workshop"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8,<9",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[project]
name = "space-customs-10-tdd-refactor"
version = "0.1.0"
description = "TDD Refactor step for the Space Customs workshop"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8,<9",
    "pytest-cov>=6,<8",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

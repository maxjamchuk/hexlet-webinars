# 08 — TDD: Red

## Цель

Зафиксировать новое требование тестом до изменения production-кода.

## Что изменилось

Добавлен тест карантина для `alien_sample`, но реализация всё ещё знает только о `hazardous`.

## Файлы для показа

- `tests/test_inspect_cargo.py` — новый тест;
- `customs.py` — намеренно недостаточная реализация.

## Подготовка

Все команды выполняются из каталога `08-tdd-red`.

```bash
uv sync --locked
```

## Запуск

```bash
uv run pytest -v
```

## Ожидаемый результат

Падение ожидаемое и настоящее: `1 failed, 12 passed`, код процесса `1`. Падает `test_alien_sample_is_sent_to_quarantine`: получено `accepted`, ожидалось `quarantine`.

## Что важно объяснить

Шаг Red состоит из трёх действий: записать новое требование, запустить тест и убедиться, что он действительно обнаруживает отсутствующее поведение.

## Ограничение текущего решения

Тест формулирует требование, но production-код его не выполняет; следующая стадия вносит минимальное изменение до зелёного состояния.


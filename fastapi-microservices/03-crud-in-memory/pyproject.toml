[project]
name = "anomaly-registry-crud-memory"
version = "0.1.0"
description = "Полный CRUD FastAPI в памяти"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115,<1",
]

[project]
name = "anomaly-registry-hello-world"
version = "0.1.0"
description = "Первое FastAPI-приложение для вебинара"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.115,<1",
]

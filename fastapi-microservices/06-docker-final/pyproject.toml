[project]
name = "anomaly-registry-docker"
version = "0.1.0"
description = "Контейнеризированный Anomaly Registry API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.14,<2",
    "fastapi[standard]>=0.115,<1",
    "sqlalchemy>=2.0,<3",
]
